# Python
__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/

# 应用运行数据（任务进度与中间文件）
outputs/

# 凭据与密钥：一律不入库
# `outputs/` 已整体忽略，这里**再显式声明一次** provider_config.json——
# 避免将来 outputs/ 规则被改动（或换成白名单写法）时，
# 把已保存的 API 凭据暴露出去。两道锁比一道稳。
outputs/provider_config.json
**/provider_config.json
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*.keystore
*.jks
id_rsa*
id_ed25519*
id_ecdsa*
credentials.json
service-account*.json
secrets.toml
.netrc
.pypirc
.npmrc
*.token
*.secret

# 本机安装锁：内含**绝对安装路径**，属机器本地状态，不入库
skills/.redskill-lock.json

# 评测（Evaluation）：结果目录、基线 worktree、本地临时文件一律不入库
eval/results/
eval/.worktrees/
eval/run-local/

# 人工术语审核表与审核结果：含真实上下文与决策，local-only
eval/term_audit.csv
eval/approved_glossary.json

# Streamlit
.streamlit/

# 本地界面截图、渲染探针与临时文档
tmp/
.codex_tmp/

# 回归 runner 落盘的失败日志（scripts/run_regression.py）
.regression-logs/

# 本地 capability audit reports and screenshots
.audit/

# 本地 Agent 状态（WorkBuddy 会话记忆 / 技能等）：机器本地，不属于 repo
.workbuddy/
.workbuddy-ai/

# 系统与编辑器
.DS_Store
.idea/
.vscode/
